## Citizen

> **DO NOT** explore the citizen gem source code. This reference is the
> complete user-facing API, embedded verbatim into every citizen local so
> their guidance never drifts. Keep it the single source of truth.

TODO: One paragraph — what citizen is and the problem it solves.

### What it offers

TODO: The public API — the methods/classes/DSL a host actually calls, with tiny
examples. This is what the `info` and `develop` locals answer from.

### Install

TODO: The exact, correct steps to add citizen to a host and set it up —
this is what the `install` local follows. Be specific to how citizen is
actually installed (e.g. for a Rails engine: add the gem, `bundle install`,
install + run migrations, wire any concerns/initializers). Don't leave it
generic.

### Citizen conventions

TODO: The conventions the `develop` local must enforce when doing
citizen work, so usage stays consistent across the host.
